import React from 'react';
import Navbar from "../Navbar/Navbar";

const LoginPage = () => {
    return (
        <div>
            <Navbar/>
            LoginPage
        </div>
    );
};

export default LoginPage;